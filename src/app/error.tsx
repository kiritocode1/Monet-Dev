'use client'
import { FC } from 'react'

interface errorProps {
  
}

const error: FC<errorProps> = ({}) => {
  return <div>error in page , please refresh the page , sorry for inconvienience.</div>
}

export default error